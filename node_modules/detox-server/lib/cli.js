#! /usr/bin/env node
'use strict';
var log = require('npmlog');
var DetoxServer = require('./DetoxServer');

log.addLevel('wss', 999, { fg: 'blue', bg: 'black' }, 'wss');
log.level = 'wss';

var detoxServer = new DetoxServer(8099);