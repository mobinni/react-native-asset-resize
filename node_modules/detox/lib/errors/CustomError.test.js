'use strict';describe('CustomError', function () {
  var CustomError = void 0;
  beforeEach(function () {
    CustomError = require('./CustomError');
  });

  it('new CustomError should be defined', function () {
    expect(new CustomError()).toBeDefined();
  });
});