'use strict';var argv = require('minimist')(process.argv.slice(2));

function getArgValue(key) {
  var value = argv ? argv[key] : undefined;
  return value;
}

module.exports = {
  getArgValue: getArgValue };